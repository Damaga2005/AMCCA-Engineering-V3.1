{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "amcca://schema/job/3.1.0",
  "title": "Durable job",
  "description": "Unit of durable execution. A job in LEASED or RUNNING must have lease_owner and lease_until (invariant I-05).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "id",
    "type",
    "state",
    "priority",
    "idempotency_key",
    "attempt",
    "max_attempts",
    "created_at",
    "updated_at"
  ],
  "properties": {
    "schema_version": {
      "const": "3.1.0",
      "description": "Contract version. Required on every persisted object (D-004)."
    },
    "id": {
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
    },
    "production_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "type": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64
    },
    "state": {
      "enum": [
        "QUEUED",
        "LEASED",
        "RUNNING",
        "SUCCEEDED",
        "FAILED",
        "BLOCKED",
        "UNKNOWN_EXTERNAL_STATE",
        "CANCELLED",
        "DEAD_LETTER"
      ]
    },
    "priority": {
      "type": "integer",
      "minimum": 0,
      "maximum": 5
    },
    "idempotency_key": {
      "type": "string",
      "minLength": 16,
      "maxLength": 200
    },
    "attempt": {
      "type": "integer",
      "minimum": 0
    },
    "max_attempts": {
      "type": "integer",
      "minimum": 1
    },
    "scheduled_at": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
        },
        {
          "type": "null"
        }
      ]
    },
    "deadline_at": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
        },
        {
          "type": "null"
        }
      ]
    },
    "lease_owner": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "lease_until": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
        },
        {
          "type": "null"
        }
      ]
    },
    "heartbeat_at": {
      "oneOf": [
        {
          "type": "string",
          "format": "date-time",
          "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
        },
        {
          "type": "null"
        }
      ]
    },
    "estimated_cost": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9]{1,13}\\.[0-9]{6}$",
          "description": "Decimal string, six fractional digits, NEVER negative. Used for budgets, estimates, reservations and settlements, none of which are conceptually signed quantities (V31-04). Money is NEVER a float (D-023)."
        },
        {
          "type": "null"
        }
      ],
      "description": "A cost estimate cannot be negative (V31-04)."
    },
    "reserved_cost": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9]{1,13}\\.[0-9]{6}$",
          "description": "Decimal string, six fractional digits, NEVER negative. Used for budgets, estimates, reservations and settlements, none of which are conceptually signed quantities (V31-04). Money is NEVER a float (D-023)."
        },
        {
          "type": "null"
        }
      ],
      "description": "A budget reservation cannot be negative (V31-04)."
    },
    "currency": {
      "type": "string",
      "pattern": "^[A-Z]{3}$"
    },
    "correlation_id": {
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
    },
    "causation_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "last_error_code": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^AMCCA-[A-Z]{2,4}-[0-9]{3}$"
        },
        {
          "type": "null"
        }
      ]
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "state": {
            "enum": [
              "LEASED",
              "RUNNING"
            ]
          }
        },
        "required": [
          "state"
        ]
      },
      "then": {
        "required": [
          "lease_owner",
          "lease_until"
        ],
        "properties": {
          "lease_owner": {
            "type": "string"
          },
          "lease_until": {
            "type": "string"
          }
        }
      },
      "$comment": "Invariant I-05: a job has at most one active lease, and an executing job always has one."
    }
  ]
}
