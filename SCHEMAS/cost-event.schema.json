{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "amcca://schema/cost-event/3.1.0",
  "title": "Cost event",
  "description": "Every euro that moves.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "cost_event_id",
    "production_id",
    "kind",
    "amount",
    "currency",
    "reconciliation_state",
    "pricing_snapshot_id",
    "occurred_at"
  ],
  "properties": {
    "schema_version": {
      "const": "3.1.0",
      "description": "Contract version. Required on every persisted object (D-004)."
    },
    "cost_event_id": {
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
    },
    "production_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "job_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "agent_run_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "kind": {
      "enum": [
        "ESTIMATE",
        "RESERVATION",
        "SETTLEMENT",
        "RELEASE",
        "ADJUSTMENT"
      ]
    },
    "amount": {
      "type": "string",
      "pattern": "^-?[0-9]{1,13}\\.[0-9]{6}$",
      "description": "SignedMoney (V31-04): ESTIMATE, RESERVATION and SETTLEMENT are always non-negative in practice, but ADJUSTMENT is a signed accounting correction (e.g. a provider refund or an under-billed correction), so the field type must admit a sign. Domain validation, not the schema, enforces that non-ADJUSTMENT kinds carry a non-negative value."
    },
    "currency": {
      "type": "string",
      "pattern": "^[A-Z]{3}$"
    },
    "provider": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "model_id": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "provider_request_id": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ]
    },
    "units": {
      "oneOf": [
        {
          "type": "object"
        },
        {
          "type": "null"
        }
      ]
    },
    "pricing_snapshot_id": {
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
    },
    "reconciliation_state": {
      "enum": [
        "ESTIMATED",
        "RECONCILED",
        "ESTIMATED_UNRECONCILED",
        "DISPUTED"
      ]
    },
    "budget_id": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$",
          "description": "ULID, generated locally. External identifiers are never primary keys (D-003)."
        },
        {
          "type": "null"
        }
      ]
    },
    "occurred_at": {
      "type": "string",
      "format": "date-time",
      "description": "RFC 3339 timestamp in UTC with explicit offset. format:date-time is enforced by FormatChecker at every validation call site (V31-02); it is not merely declared."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "kind": {
            "enum": [
              "ESTIMATE",
              "RESERVATION",
              "SETTLEMENT"
            ]
          }
        },
        "required": [
          "kind"
        ]
      },
      "then": {
        "properties": {
          "amount": {
            "type": "string",
            "pattern": "^[0-9]{1,13}\\.[0-9]{6}$",
            "description": "Decimal string, six fractional digits, NEVER negative. Used for budgets, estimates, reservations and settlements, none of which are conceptually signed quantities (V31-04). Money is NEVER a float (D-023)."
          }
        }
      },
      "$comment": "V31-04: only ADJUSTMENT (and RELEASE, structurally a reduction represented as its own non-negative magnitude) may be negative; the common kinds cannot."
    }
  ]
}
