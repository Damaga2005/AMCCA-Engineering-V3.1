<UserControl x:Class="AMCCA.App.Views.JobQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1000">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Job Queue (SPEC/14)" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Toolbar -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableStates}"
                          SelectedItem="{Binding SelectedState}"
                          Background="#1E1E2E" Foreground="#11111B" Padding="8" Margin="0,0,10,0"/>

                <TextBlock Grid.Column="1" Text="{Binding PageSummary}" Foreground="#A6ADC8"
                           VerticalAlignment="Center" Margin="10,0"/>

                <Button Grid.Column="2" Content="Requeue Dead-Lettered"
                        Command="{Binding RequeueDeadLetterJobCommand}"
                        Background="#F9E2AF" Foreground="#11111B" FontWeight="Bold" Padding="15,8" Margin="0,0,10,0"/>

                <Button Grid.Column="3" Content="Refresh" Command="{Binding RefreshCommand}"
                        Background="#45475A" Foreground="#CDD6F4" Padding="15,8"/>
            </Grid>
        </Border>

        <!-- Job Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Jobs}" SelectedItem="{Binding SelectedJob}"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  EnableRowVirtualization="True"
                  Background="#313244" Foreground="#CDD6F4" RowBackground="#1E1E2E"
                  AlternatingRowBackground="#181825" BorderThickness="0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Job ID" Binding="{Binding Id}" Width="180"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="130"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="130"/>
                <DataGridTextColumn Header="P" Binding="{Binding Priority}" Width="40"/>
                <DataGridTextColumn Header="Attempt" Binding="{Binding Attempt}" Width="70"/>
                <DataGridTextColumn Header="Max" Binding="{Binding MaxAttempts}" Width="50"/>
                <DataGridTextColumn Header="Production" Binding="{Binding ProductionId}" Width="160"/>
                <DataGridTextColumn Header="Lease Owner" Binding="{Binding LeaseOwnerId}" Width="130"/>
                <DataGridTextColumn Header="Lease Until" Binding="{Binding LeaseUntil}" Width="160"/>
                <DataGridTextColumn Header="Fence" Binding="{Binding FenceToken}" Width="60"/>
                <DataGridTextColumn Header="Updated At" Binding="{Binding UpdatedAt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Paging -->
        <Border Grid.Row="3" Background="#313244" CornerRadius="8" Padding="10" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="&#x2190; Previous" Command="{Binding PreviousPageCommand}"
                        Background="#45475A" Foreground="#CDD6F4" Padding="15,6" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding PageSummary}" Foreground="#CDD6F4" VerticalAlignment="Center" Margin="10,0"/>
                <Button Content="Next &#x2192;" Command="{Binding NextPageCommand}"
                        Background="#45475A" Foreground="#CDD6F4" Padding="15,6" Margin="10,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
