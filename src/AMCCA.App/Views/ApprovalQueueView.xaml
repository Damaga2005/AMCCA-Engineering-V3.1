<UserControl x:Class="AMCCA.App.Views.ApprovalQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Human Approval Queue (SPEC/09)" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Approvals Grid: SPEC/60 obligation 5 -- every approval request shows the exact action,
             subject, cost ceiling and expiry being approved, not just its id and timestamps. -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Approvals}" SelectedItem="{Binding SelectedApproval}" AutoGenerateColumns="False" CanUserAddRows="False" Background="#313244" Foreground="#CDD6F4" RowBackground="#1E1E2E" AlternatingRowBackground="#181825" BorderThickness="0" HeadersVisibility="Column" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Approval ID" Binding="{Binding Id}" Width="140"/>
                <DataGridTextColumn Header="Production ID" Binding="{Binding ProductionId}" Width="160"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="130"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding SubjectDisplay}" Width="150"/>
                <DataGridTextColumn Header="Cost Ceiling" Binding="{Binding CostCeilingDisplay}" Width="100"/>
                <DataGridTextColumn Header="Expires At" Binding="{Binding ExpiresAt}" Width="160"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="90"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Actions Panel -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding ApprovalReason, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E2E" Foreground="#CDD6F4" Padding="8" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Approve" Command="{Binding ApproveCommand}" Background="#A6E3A1" Foreground="#11111B" FontWeight="Bold" Padding="20,8" Margin="0,0,10,0"/>
                <Button Grid.Column="2" Content="Reject" Command="{Binding RejectCommand}" Background="#F38BA8" Foreground="#11111B" FontWeight="Bold" Padding="20,8"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
