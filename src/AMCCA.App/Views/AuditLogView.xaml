<UserControl x:Class="AMCCA.App.Views.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Forensic Audit Log (SPEC/02)" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Filter Bar -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding FilterQuery, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E2E" Foreground="#CDD6F4" Padding="8" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Refresh" Command="{Binding RefreshCommand}" Background="#45475A" Foreground="#CDD6F4" Padding="15,8"/>
            </Grid>
        </Border>

        <!-- Audit Log Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Entries}" AutoGenerateColumns="False" CanUserAddRows="False" Background="#313244" Foreground="#CDD6F4" RowBackground="#1E1E2E" AlternatingRowBackground="#181825" BorderThickness="0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding OccurredAt}" Width="160"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="160"/>
                <DataGridTextColumn Header="Actor" Binding="{Binding ActorId}" Width="120"/>
                <DataGridTextColumn Header="Subject" Binding="{Binding SubjectId}" Width="180"/>
                <DataGridTextColumn Header="Outcome" Binding="{Binding Outcome}" Width="100"/>
                <DataGridTextColumn Header="Audit ID" Binding="{Binding AuditId}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
