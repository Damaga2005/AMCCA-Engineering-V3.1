<UserControl x:Class="AMCCA.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="System Dashboard" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Metrics Cards -->
        <UniformGrid Grid.Row="1" Columns="3" Margin="0,0,0,25">
            <Border Background="#313244" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Active Productions" Foreground="#A6ADC8" FontSize="14"/>
                    <TextBlock Text="{Binding ActiveProductionsCount}" Foreground="#89B4FA" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="#313244" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Pending Approvals" Foreground="#A6ADC8" FontSize="14"/>
                    <TextBlock Text="{Binding PendingApprovalsCount}" Foreground="#F9E2AF" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
            <Border Background="#313244" CornerRadius="8" Padding="15" Margin="5">
                <StackPanel>
                    <TextBlock Text="Verified Publications" Foreground="#A6ADC8" FontSize="14"/>
                    <TextBlock Text="{Binding VerifiedPublicationsCount}" Foreground="#A6E3A1" FontSize="32" FontWeight="Bold" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Quick Actions -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="8" Padding="20">
            <StackPanel>
                <TextBlock Text="Quick Actions" FontSize="18" FontWeight="SemiBold" Foreground="#CDD6F4" Margin="0,0,0,15"/>
                <WrapPanel>
                    <Button Content="+ New Production" Command="{Binding NewProductionCommand}" Background="#89B4FA" Foreground="#11111B" FontWeight="Bold" Padding="15,8" Margin="0,0,10,10"/>
                    <Button Content="Review Approvals" Command="{Binding ViewApprovalsCommand}" Background="#F9E2AF" Foreground="#11111B" FontWeight="Bold" Padding="15,8" Margin="0,0,10,10"/>
                    <Button Content="Refresh Dashboard" Command="{Binding RefreshCommand}" Background="#45475A" Foreground="#CDD6F4" Padding="15,8" Margin="0,0,10,10"/>
                </WrapPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
