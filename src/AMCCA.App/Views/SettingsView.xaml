<UserControl x:Class="AMCCA.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="System Settings" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Form -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="20">
            <StackPanel>
                <TextBlock Text="Package Version" Foreground="#A6ADC8" FontSize="14"/>
                <TextBlock Text="{Binding PackageVersion}" Foreground="#CDD6F4" FontSize="16" FontWeight="Bold" Margin="0,5,0,15"/>

                <TextBlock Text="Secret Store Reachability" Foreground="#A6ADC8" FontSize="14"/>
                <TextBlock Text="{Binding SecretStoreAvailable}" Foreground="#A6E3A1" FontSize="16" FontWeight="Bold" Margin="0,5,0,15"/>

                <TextBlock Text="Global Autonomous Kill Switch (SPEC/04)" Foreground="#A6ADC8" FontSize="14"/>
                <CheckBox Content="Activate Global Kill Switch" IsChecked="{Binding GlobalKillSwitch}" Foreground="#F38BA8" FontSize="16" Margin="0,5,0,20"/>
            </StackPanel>
        </Border>

        <!-- Save Button -->
        <Button Grid.Row="2" Content="Save Settings" Command="{Binding SaveSettingsCommand}" Background="#89B4FA" Foreground="#11111B" FontWeight="Bold" Padding="20,10" HorizontalAlignment="Right" Margin="0,15,0,0"/>
    </Grid>
</UserControl>
