<UserControl x:Class="AMCCA.App.Views.ProductionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Background="#1E1E2E" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Productions Management" FontSize="26" FontWeight="Bold" Foreground="#CDD6F4" Margin="0,0,0,20"/>

        <!-- Creation Bar -->
        <Border Grid.Row="1" Background="#313244" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding NewTopic, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E2E" Foreground="#CDD6F4" Padding="8" Margin="0,0,10,0"/>
                <ComboBox Grid.Column="1" Text="{Binding NewNiche}" Background="#1E1E2E" Foreground="#CDD6F4" Margin="0,0,10,0">
                    <ComboBoxItem Content="tech" IsSelected="True"/>
                    <ComboBoxItem Content="finance"/>
                    <ComboBoxItem Content="history"/>
                    <ComboBoxItem Content="science"/>
                </ComboBox>
                <Button Grid.Column="2" Content="+ Create" Command="{Binding CreateProductionCommand}" Background="#89B4FA" Foreground="#11111B" FontWeight="Bold" Padding="15,8"/>
            </Grid>
        </Border>

        <!-- Data Grid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Productions}" SelectedItem="{Binding SelectedProduction}" AutoGenerateColumns="False" CanUserAddRows="False" Background="#313244" Foreground="#CDD6F4" RowBackground="#1E1E2E" AlternatingRowBackground="#181825" BorderThickness="0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="180"/>
                <DataGridTextColumn Header="Topic" Binding="{Binding Topic}" Width="*"/>
                <DataGridTextColumn Header="Niche" Binding="{Binding Niche}" Width="100"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" Width="120"/>
                <DataGridTextColumn Header="Created At" Binding="{Binding CreatedAt}" Width="160"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
