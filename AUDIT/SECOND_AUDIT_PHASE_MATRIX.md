# AMCCA Engineering V3.1 — Second Forensic Audit: Phase Matrix

> **Modo:** RED TEAM / RELEASE BLOCKER / ZERO TRUST  
> **Fecha:** 2026-09-03  
> **Commit Auditado:** `d1d9fefe32be33fc0b34130598cb535a3bc6e398`  
> **Estados permitidos:** PASS, FAIL, BLOCKED  

---

| Phase | Required | Evidence Expected | Evidence Found | Tests Found | Integration Verified | Stub-free | BUILD_ORDER Compliant | Status |
|---|---|---|---|---|---|---|---|---|
| **1 — Repository, CI, package validator** | Spec cannot silently drift; CI runs green on every commit; fails on test/build/validator failure. | Remote CI execution logs proving `.github/workflows/validation.yml` executes and fails appropriately on Ubuntu/Windows. | `.github/workflows/validation.yml` configurado para `ubuntu-latest`. No hay logs de ejecución de GitHub Actions disponibles en el entorno local (sin CLI `gh` ni token remoto). | `TOOLS/validate_package.py` (57 checks), tests de repo en `TOOLS/test_*.py`. | NO (no verificable remotamente) | YES | NO (no valida desktop Windows en CI) | **FAIL** |
| **2 — Configuration, secrets, preflight** | Invalid config aborts startup; literal secret aborts startup; safe defaults; gateway unverified by default. | Runtime validation enforcing rejection of missing files, bad YAML, and raw secrets. | `ConfigManager.cs` valida esquemas y secretos; `PreflightService.cs` ejecuta preflight fail-closed. | `ConfigurationContractTests.cs`, `PreflightContractTests.cs`, `SecretStoreContractTests.cs`. | YES | YES | YES | **PASS** |
| **3 — Database, migrations, event store, audit store** | Durable, versioned, append-only history; all 58 tables from `tables.json`; upgrade, rollback, restore tests. | All tests execute against canonical schema created via migrations. | 9 suites de pruebas crean esquemas manualmente con `CREATE TABLE IF NOT EXISTS` en lugar de usar `MigrationService.UpgradeAsync()`. Triggers físicos bloquean UPDATE/DELETE en `events`/`audit_log`. | `CanonicalMigrationSchemaTests.cs`, `DatabaseAndMigrationContractTests.cs`, `EventsAppendOnlyPhysicalEnforcementRegressionTests.cs`. | NO (bypasses en 9 suites de tests) | YES | NO (violación de fuente única de verdad en tests) | **FAIL** |
| **4 — Domain model and state machine** | Illegal transitions impossible; every transition in `SPEC/13` has a test; every non-listed transition rejected with `AMCCA-STM-001`. | `StateMachineRegistry.cs` carga `SCHEMAS/state-machine.json`; `ProductionService.cs` ejecuta TX-1 atómica. | Matriz canónica cargada y validada en constructor; transiciones no listadas rechazadas; `TransitionAsync` asegura rollback en fallos. | `StateMachineContractTests.cs`, `ArchitectureAndRegistryRegressionTests.cs`. | YES | YES | YES | **PASS** |
| **5 — Jobs, leases, idempotency, recovery, reconciliation** | Work survives crashes without duplicating; stale worker cannot mutate state; fence tokens validated on fail/complete. | `JobManager.cs` valida lease y fence token de forma atómica en `CompleteJob` y `FailJob`. | Fencing token validado en `CompleteJobOrThrowAsync` y `FailJobAsync`; heartbeat condicional; estado `UNKNOWN` no reintentable sin reconciliación. | `JobsAndLeasesContractTests.cs`, `JobLeaseFenceAndHeartbeatRegressionTests.cs`. | YES | YES | YES | **PASS** |
| **6 — Tool registry and agent runtime** | Agents cannot exceed contract; forbidden tool blocked and audited; cost and timeout strictly enforced. | `ToolRegistry.cs` y `AgentRuntime.cs` validan permisos, unicidad, timeouts y costos. | `ToolRegistry.RegisterTool` rechaza duplicados; `AgentRuntime` bloquea tools prohibidas y audita intentos; timeout y max_cost verificados. | `ToolRegistryAndAgentRuntimeContractTests.cs`, `AgentContractEnforcementRegressionTests.cs`, `ArchitectureAndRegistryRegressionTests.cs`. | YES | YES | YES | **PASS** |
| **7 — Provider gateway port + first adapter + model registry** | AI capability behind abstraction; probe capability before enabling; no mock implementation in production. | Live HTTP requests over wire/socket; auth headers, payload parsing, token counting. | `DirectOpenAiCompatibleGatewayAdapter.cs` y `OmniRoutersGatewayAdapter.cs` implementan HTTP real, pero la suite `AiProviderRealIntegrationTests.cs` usa `ControlledHttpMessageHandler` en memoria en lugar de endpoints de red/loopback reales. | `ProviderGatewayAndModelRegistryContractTests.cs`, `AiProviderRealIntegrationTests.cs`. | NO (sin pruebas contra sockets reales) | YES | NO (incumple requisito de prueba HTTP real) | **FAIL** |
| **8 — Research, claims, sources, trends, opportunity scoring** | Evidence plane exists; material claim without sufficient sources cannot reach `VERIFIED`; SSRF protection with coupled DNS. | `ResearchService.cs` enlaza fuentes con claims; `SsrfValidator.cs` protege contra DNS rebinding y TOCTOU. | Enlace de claims y fuentes implementado, pero `SsrfValidator.CreateSafeSocketsHttpHandler()` está desconectado del código de producción de scraping/fetch (código muerto). | `ResearchAndClaimValidationContractTests.cs`, `SsrfAndDnsRebindingRegressionTests.cs`. | NO (handler SSRF desconectado de producción) | YES | NO (SSRF desalineado de la capa de fetch) | **FAIL** |
| **9 — Script, storyboard, assets, voice, render** | Production pipeline produces artifacts with lineage; artifact DAG is complete and acyclic. | Lineage tracking, prompt versioning immutable, path confinement on render. | `ArtifactManager.cs`, `PromptManager.cs`, `MediaRenderer.cs` con confinamiento estricto de rutas y hashes deterministas. | `ContentPipelineAndPromptContractTests.cs`, `MediaPipelineAndDisclosureContractTests.cs`, `PathConfinementAndSafeArchiveRegressionTests.cs`. | YES | YES | YES | **PASS** |
| **10 — Deterministic QA, AI-assisted QA, rights, duplicates** | Quality gates that AI cannot self-certify; PASS verdict unreachable using AI findings alone (`AMCCA-QA-002`). | `QaVerdictEvaluator.cs` exige verificaciones deterministas y prohíbe PASS con severidad CRITICAL o dimensiones < 8.0. | `QaVerdictEvaluator.cs` implementa las reglas I-19 y D-024 bloqueando veredictos PASS sin checks deterministas. | `QaEngineAndDagReworkContractTests.cs`. | YES | YES | YES | **PASS** |
| **11 — Rework and DAG invalidation** | Failure has a defined path; every QA failure produces targeted, bounded rework; invalidates downstream artifacts without deleting. | `DagReworkResolver` calcula el subgrafo de artefactos descendientes a invalidar a partir del fallo QA. | `DagReworkResolver` en `ArtifactDag.cs` solo implementa un contador `CanAttemptRework`. No mapea los fallos de QA a los nodos correspondientes ni calcula la invalidación del DAG en producción. | `QaEngineAndDagReworkContractTests.cs`. | NO (resolución de retrabajo incompleta en runtime) | YES | NO (lógica de invalidación no integrada en ciclo de vida) | **FAIL** |
| **12 — Platform hub, OAuth, capability matrix, publishing** | Publication with verification, not optimism; OAuth lifecycle; capability matrix; no duplicate publication under chaos. | Adaptadores de plataforma para YouTube/TikTok/Instagram; flujo OAuth; verificación de evidencia autoritativa externa. | `PlatformHub.cs` solo inserta en base de datos (`platform_accounts`, `publications`). No existen adaptadores reales de plataforma ni gestión de OAuth ni matriz de capacidades. | `PublishingAndPlatformHubContractTests.cs`. | NO (sin adaptadores de plataforma ni OAuth) | YES | NO (adaptadores externos y OAuth no implementados) | **FAIL** |
| **13 — Synthetic-content disclosure and compliance gate** | Lawful, labelled publication; required label unapplied blocks intent with `AMCCA-CMP-001`; C2PA manifests. | `SyntheticContentService.cs` y `ComplianceGate.cs` validan presencia de etiquetas obligatorias y manifiesto C2PA. | `ComplianceGate.cs` bloquea publicaciones sin declaración obligatoria arrojando `AMCCA-CMP-001`. | `MediaPipelineAndDisclosureContractTests.cs`. | YES | YES | YES | **PASS** |
| **14 — Monetization, attribution, analytics, revenue** | Money that is measured, not guessed; exact decimal arithmetic; estimate cannot enter revenue ledger. | `RevenueService.cs` y `BudgetManager.cs` usan aritmética `decimal` estricta; triggers/checks en base de datos. | Aritmética decimal estricta (`Money.Format`, `decimal`); tabla `revenue_events` tiene constraint `provenance != 'ESTIMATED'`. | `MonetizationAndRevenueContractTests.cs`, `MoneyPrecisionAndDecimalContractTests.cs`. | YES | YES | YES | **PASS** |
| **15 — Memory, genome, experiments** | Learning from measured outcomes; a niche reaches PROVEN only from measured data. | Servicios de producción (`GenomeService`, `MemoryService`) que evalúan métricas reales para transicionar nichos a `PROVEN`. | **INEXISTENTE.** No hay código de producción en `src/` que gestione nichos, genomas ni experimentos. `PROVEN` solo existe como string en la migración DDL de SQLite. | No existen tests para Fase 15. | NO | YES | NO (código y tests ausentes en producción) | **FAIL** |
| **16 — Desktop UI and inspectors** | Operator can see and stop everything; authentic WPF desktop application with MVVM, DI, navigation, views. | Proyecto WPF (`AMCCA.App.csproj` con `<UseWPF>true</UseWPF>`), vistas XAML, ViewModels, DI, binding. | **INEXISTENTE COMO WPF.** `src/AMCCA.App` es una aplicación de consola rudimentaria (`Program.cs` con `Console.WriteLine`). No hay XAML, no hay MVVM, no hay vistas de Dashboard ni Inspectores. | No existen tests de UI. | NO | YES | NO (violación directa de SPEC/65 y SPEC/66) | **FAIL** |
| **17 — Chaos, concurrency, security suites** | Guarantees are real: SPEC/72 (Security), SPEC/73 (Concurrency: C-01 a C-14), SPEC/74 (Chaos: X-01 a X-16) ejecutadas individualmente. | Suites de prueba dedicadas ejecutando escenarios de caída de proceso (kill -9), carreras de leases, particiones y saturación SQLite. | Existen pruebas de seguridad de SPEC/72, pero SPEC/73 (C-01 a C-14) y SPEC/74 (X-01 a X-16) carecen de suites ejecutables dedicadas e individuales en `tests/`. | `SecurityHardeningSpec72ContractTests.cs` existe; SPEC/73 y SPEC/74 ausentes como suites formales. | NO | YES | NO (suites de caos y concurrencia incompletas) | **FAIL** |
| **18 — Packaging, installer, signing, release validation** | Shippable artifact: self-contained .NET 8 `win-x64` (`AMCCA.exe`), instalador WiX MSI (`AMCCA-Setup.exe`), release manifest SHA-256. | Binario `AMCCA.exe`, proyecto WiX con fuentes `.wxs`, generación de MSI y prueba de clean install/uninstall. | `dotnet publish` compila `AMCCA.exe` (consola). No existe instalador WiX MSI (`AMCCA-Setup.exe`), no hay archivos `.wxs`, no se realizaron pruebas de instalación/desinstalación. | `PackagingVerificationRegressionTests.cs` (solo valida XML estático). | NO | YES | NO (empaquetado e instalador incompletos) | **FAIL** |
